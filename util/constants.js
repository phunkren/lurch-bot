const BOT_PREFIX = '!roll';

const COMMAND_HELP = 'help';

const COMMAND_FALLBACK =
  'command not recognised. Try **!roll help** for a list of available commands.';

module.exports = {
  BOT_PREFIX,
  COMMAND_HELP,
  COMMAND_FALLBACK,
};
